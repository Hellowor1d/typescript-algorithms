interface TreeNode {
    value : number
    children?: TreeNode[];
}
//TODO·
function getTreeDeepPath(tree:TreeNode[],parent:TreeNode[]){
    for(let i=0, len=tree.length; i<len; i++){
        if(tree[i].children){

        }
    }
    
}