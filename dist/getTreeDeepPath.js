"use strict";
//TODO·
function getTreeDeepPath(tree, parent) {
    for (let i = 0, len = tree.length; i < len; i++) {
        if (tree[i].children) {
        }
    }
}
//# sourceMappingURL=getTreeDeepPath.js.map